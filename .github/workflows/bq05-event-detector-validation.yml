name: BQ05 event detector validation

on:
  push:
    branches: [main]
    paths: ['.github/workflows/bq05-event-detector-validation.yml']

permissions:
  contents: read

env:
  VERIFY_DAYTONA: '0'
  ALLOW_DAYTONA_MUTATION: '0'
  PYTHON_DOTENV_DISABLED: '1'

jobs:
  candidate:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Restore exact retained patch and verification scripts
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          test "$(git rev-parse HEAD^)" = ce16ac4cbe9fd98f41d679515c30819b647c5693
          python - <<'PY'
          import base64, hashlib, json, lzma, os, pathlib, subprocess, urllib.request
          request = urllib.request.Request('https://api.github.com/repos/ms81labs/sol-astra-football-analyses-sept26/git/blobs/00f129f79b3550ed45c53709370fd9edcfac3bcf', headers={'Authorization': 'Bearer ' + os.environ['GH_TOKEN'], 'Accept': 'application/vnd.github+json'})
          with urllib.request.urlopen(request, timeout=60) as response:
              packed = base64.b64decode(json.load(response)['content'])
          assert hashlib.sha256(packed).hexdigest() == '9ed2ec8bfc9ccc17d3aaf6253a8051ffa3a1217f9f592b894898f84d413446a9'
          payload = json.loads(lzma.decompress(packed))
          assert set(payload) == {'candidate.patch', 'manifest.json', 'runner.py', 'compare_sources.py', 'differential.py', 'mutation_check.py'}
          folder = pathlib.Path('.bq05-events')
          folder.mkdir()
          for name, text in payload.items():
              (folder / name).write_text(text)
          manifest = json.loads(payload['manifest.json'])
          assert hashlib.sha256((folder / 'candidate.patch').read_bytes()).hexdigest() == manifest['patchSha256']
          original = pathlib.Path('backend/app/analytics.py').read_bytes()
          assert hashlib.sha256(original).hexdigest() == '787c020cc133d46d51bd38c2a24b8afd67040e0aa8bcd1811a782398241e90d2'
          (folder / 'analytics-before.py').write_bytes(original)
          (folder / 'ruff-baseline-before.json').write_bytes(pathlib.Path('backend/quality/ruff-baseline.json').read_bytes())
          subprocess.run(['git', 'apply', '--check', str(folder / 'candidate.patch')], check=True)
          subprocess.run(['git', 'apply', '--include=backend/tests/test_event_detection_contract.py', '--include=backend/quality/ruff-baseline.json', str(folder / 'candidate.patch')], check=True)
          PY
      - name: Install required media executables
        run: sudo apt-get update && sudo apt-get install -y ffmpeg
      - name: Install unchanged locked runtime and isolated quality profiles
        run: |
          python -m pip install --require-hashes -r backend/requirements/dev.lock
          python -m pip install -e . --no-deps
          python -m venv .quality-tools
          .quality-tools/bin/python -m pip install --require-hashes -r backend/requirements/quality-linux.lock -r backend/requirements/typing-linux.lock
          python - <<'PY'
          import json, sys
          from pathlib import Path
          from importlib.metadata import version
          assert sys.version_info[:2] == (3, 11)
          versions = {name: version(name) for name in ('fastapi', 'pydantic', 'anyio')}
          assert versions == {'fastapi': '0.121.0', 'pydantic': '2.13.5', 'anyio': '4.15.1'}, versions
          Path('.bq05-events/runtime.json').write_text(json.dumps({'python': sys.version, 'packages': versions}, indent=2) + '\n')
          PY
      - name: Characterize original behavior and reproduce structural gate failure
        run: python .bq05-events/runner.py before
      - name: Apply candidate and prove original control flow and exact diagnostic retirement
        run: python .bq05-events/runner.py apply
      - name: Verify differential cases, mutation sensitivity, memory-limit test and full OpenAPI equality
        run: python .bq05-events/runner.py after
      - name: Verify unchanged quality configuration and clean typing
        run: python .bq05-events/runner.py quality
      - name: Retain verified Git objects without moving any branch
        env:
          GH_TOKEN: ${{ github.token }}
        run: python .bq05-events/runner.py retain
      - name: Retain source-bound validation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bq05-event-detector-validation
          path: .bq05-events/
          include-hidden-files: true
          retention-days: 30
          if-no-files-found: error
