# C06 completion — declared media/process operating envelope

## Authority and scope
Continue the approved `Guerilla_V3_1_Execution_Plan.md` §10, R09/narrowed N02 and V3T43–49. User authorised delivery onto the only `main` branch. C04/C05 and historical audit documents are retained. No new decoder default, ML inference, provider/paid/GPU operation, deployment or live-store change. Final v3.1 V3T50/browser and football accuracy are separate.

## Recovered design
Reuse the existing FFmpeg adapter, executable trust resolver and Python stdlib. A post-exec launcher installs configured CPU/AS/file limits and emits an independent limit receipt; the controller supervises deadlines, demand/stall, output caps and process groups. Direct decode separately bounds resident payload assembly and cumulative work. Probe/proxy/export share source admission, scoped process execution and staged publication. Offline-match/live and unsupported process platforms remain explicitly refused, not implicitly qualified. Rlimits/protocol allowlists are not filesystem/network isolation.

## Execution sequence
1. Recover immutable candidate and reproduce baseline counterexamples. Preserve original CI/verifier/locks/caps and honest environment-specific failures.
2. Run actual child/FFmpeg regressions; self-review all normal callers and source/receipt boundaries. Reproduce any new findings before a minimal shared-boundary fix.
3. Freeze final candidate. Run dedicated media suite, opt-in >8GiB generated decode, full original code-only verifier and integration/real-media lanes. Retain actual logs/JUnit/source and environment digests.
4. Publish exact verified product tree on main, preserve all source history and verify only main exists. Record source-bound closure, migration/rollback scope and disabled/unqualified capabilities. No claim of final project acceptance or independent review.

## Acceptance map
- V3T43: configured child limits, policy digest and modes; no hidden fixed override.
- V3T44: real CPU soft/hard, address-space and file limits; unrelated SIGKILL remains unknown cause.
- V3T45: independent buffering/work ceilings; generated decode beyond prior8GiB boundary and strict caller cap failure.
- V3T46: slow consumer, active stall, paused-consumer global deadline, cancellation and generator-close reaping.
- V3T47: common process policy for probe/proxy/export; source/path preservation and no partial publication on failure.
- V3T48: integer/fractional/duplicate/missing PTS, rotation/source coordinates and independent capability controls.
- V3T49: actual supported Linux profile and executable versions; macOS runtime/GPU unqualified.

Implementation progress and exact evidence are recorded in the companion closure, not inferred from checkboxes. Synthetic media tests are not full-match football performance qualification. CPU seconds are not footage seconds; AS is not RSS; file limits do not limit stdout pipes.
