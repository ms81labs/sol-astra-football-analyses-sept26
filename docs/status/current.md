# Current status

This is the one authoritative current-status page. The application accepts a football match upload and owns orchestration, analytics, results, validation, persistence, and presentation. Daytona is limited to private, ephemeral GPU execution of the sealed worker job.

The active remote provider is Daytona. RunPod was retired on 2026-09-08 after provider-neutral and Daytona parity tests were established. Historical RunPod records remain under `docs/archive/` and in Git history; the legacy `runpod_run_id` SQLite column and historical artifact readers remain for compatibility only.

The first authorized Daytona smoke on 2026-09-09 failed during image build. The corrected second attempt allocated a private sandbox, then failed during remote execution; deleted-sandbox telemetry cannot recover the exact failed command boundary or stderr. A post-failure reproduction in that attempt's exact proof image confirmed missing OpenCV runtime libraries. The S4 correction added and locally proved those libraries in the replacement worker image.

The third authorized attempt on 2026-09-10 passed every no-mutation preflight but exited before sandbox allocation because `/usr/bin/python3` could not import the pinned `daytona==0.207.0` host SDK. The redirected result is empty, the safe stderr is `Daytona client construction failed`, and a direct non-mutating factory reproduction confirms `ModuleNotFoundError: No module named 'daytona'`. Daytona listed zero sandboxes both before and after the attempt; no cleanup, registry mutation, or RunPod mutation occurred.

The fourth authorized attempt on 2026-09-10 used the guarded SDK-ready environment and passed. Daytona supplied an `RTX-PRO-6000`; `nvidia-smi`, worker imports, and the bounded fixture all exited zero. Cleanup succeeded on its first attempt, deletion was confirmed, and the current independent listing contains zero sandboxes. No RunPod or registry mutation occurred.

That fourth attempt is historical infrastructure evidence bound to its original source. It is not product acceptance for the current source and must not be repeated. No fifth infrastructure smoke was run.

```bash
VERIFY_DAYTONA=0 ALLOW_DAYTONA_MUTATION=0 scripts/verify.sh
python3 -m backend.scripts.run_daytona_gpu_smoke --dry-run
```

Historical command records render the host credential only as `DAYTONA_API_KEY=<redacted>`; never place it in a sandbox environment or committed file. The G-PRODUCT credential remained host-only and was not persisted. Rotate it after this acceptance because it was shared through chat.

No RunPod image was pushed or deployed during this migration. No Git history was rewritten. No model or application data was deleted.

## Local remediation status

All R01–R18 implementation units in the [full remediation plan](../superpowers/plans/2026-09-12-full-project-remediation.md) are complete and independently reviewed. The repository-wide sweep accounted for all 4,798 tracked paths and parsed all 659 tracked Python files with zero parse failures; this structural coverage is not a claim of manual semantic review of every line.

The latest local provider-disabled release baseline binds source `40eade8727c0eed69cbb6b397bac0ea693103f02` through the v7.3 manifest. Verification commit `973d78d4d7f6a396cf9ca934d2d18ce92297c4e0`, manifest digest `c00dc0c7278463f62ed532c05f5673d9f8ab5af43b4a0d1b0d3f71ea12deb7a2`, receipt digest `0020b826f48ad7ea11b66cec9254de74b902551317a70159ead2245b6d05bb78`, and pre-cloud evidence digest `5fe693eaafd1999e52c80c38f8fffc5b97aaf5739cf197ba508c71987513e94a` are recorded locally. All five declared model/seed assets match their manifest hashes. Earlier archives remain preserved. The tracked `backend/release/verification/v7.3-pre-cloud.json` is byte-identical to its ignored current copy at `backend/storage/verification-evidence-github-handoff-20260916-current/v7.3-pre-cloud.json`. Exact build-only preflight passed for this source and these five assets without a provider call. The pre-cloud evidence keeps infrastructure smoke pending and `overallPassed=false`. Exact provider-disabled verifier results for the new binding were:

- backend: `3,682 passed; 1 skipped` (one upstream TestClient deprecation warning)
- sidecar: `100 passed`
- frontend: `144 passed across 26 test files`
- release manifest: `129 passed`
- runtime options: `67 passed`
- negative preflight: `105 passed; 34 deselected`
- frontend lint, both TypeScript checks, production build, and backend startup: passed
- production npm audit: `0 vulnerabilities`

| Evidence class | Result and provenance | Readiness consequence |
|---|---|---|
| Software verification | All 12 local gates and build-only preflight passed for release source `40eade8727c0eed69cbb6b397bac0ea693103f02`; exact counts are listed above. | Ready for continued development and controlled evaluation within test coverage. |
| Actual pipeline inference | Runtime source `b82a43c82d68f644846f7e1c9eda5eaa81845dcd` completed **local pinned-dependency CPU** sealed runs on the declared one- and five-minute clips. The five-minute result imported 15,880 rows, 1,516 sampled frames, 694 accepted ball rows and 51 provisional events. Later sources through `40eade8727c0eed69cbb6b397bac0ea693103f02` change packaging, retired/evaluation scripts, trust-crop review scoring, import-time display detection, frontend review behavior and accessibility, storage durability, bounded SoccerNet acquisition, analytics-summary indexing and release-evidence monitoring without changing inference math; none was rerun through the sealed worker. | Runtime behavior is verified at five minutes on an earlier source, but no exact-current-source sealed inference, Daytona/GPU acceptance or independent accuracy evidence. |
| Independently labeled accuracy | No acceptance-qualified HOTA, IDF1, visible-ball precision/recall, pitch error, possession agreement, or event AP result. Validation-only pitch diagnostics below fail the pilot bar. | Unproven; workflow, crop checks, and validation-only diagnostics are excluded from acceptance denominators. |
| Resource/capacity measurement | Earlier G-CAPACITY execution completed both intact `117092` halves sequentially through the production command in 8,378.199 and 8,145.772 seconds end to end. Worst measured peaks were 2.219 GiB worker RSS, 1.008 GiB host RSS, 849 MiB total sandbox GPU use, 6.452 GiB local staging and 8.000 GiB remote staging, with empty monitor errors and confirmed cleanup. It was not rerun for this new source. | Historical capacity pass for this representative two-half match inside the 8 GiB RAM / 10 GiB disk / 9,000-second execution / 180-minute TTL policy, not output accuracy or arbitrary-source scale. |
| Analyst acceptance | No declared-camera pilot has been reviewed against agreed thresholds by the intended analyst. | Unproven; keep outputs review-only. |

A scoring-boundary investigation found that 13 / 18 frozen task windows begin off the protocol v3 source-global evaluation grid. The native production pipeline samples clip-local frames `0, step, 2×step, ...`; naïvely inferring on those unchanged annotation clips would give zero exact scored-frame overlap for the off-grid tasks. The shared evaluator rejects that scope without an explicit, mathematically aligned source start and records the declared start in the task result; a wrong start and a clip override on `whole-source` artifacts are rejected. A read-only schedule check maps aligned source clips to all 9,297 frozen evaluation frames across 18 tasks. The two off-grid `117092` validation tasks already use intact-half `whole-source` IDs, and all three `117093` validation tasks start on-grid. All 11 off-grid held-out short-clip tasks have separate **media-only** aligned MP4s built from real source-prefix frames; five parent-video hashes and all 11 annotation and aligned-video hashes match the frozen/local manifests, exact frame counts and dimensions hold, and each start/midpoint/end anchor corresponds to its annotation clip. Representative Watertown, VHS and 4K midpoint images were visually intact. The ignored [aligned-media manifest](../../backend/storage/trained_detector_candidates/touchline_detector_candidate_v7/football_analysis_pilot_corpus_v1/inference_clips/aligned_media_manifest_v1.tsv) records the source start, prefix and clip hashes. Future sealed-worker imports persist `input_video_identity.json` from the validated job receipt, and the held-out task CLI hashes the fixed frozen clip bytes, checks their manifest hashes/metadata and requires the imported hash to match; source CLI rechecks the saved task hash and aligned start. Legacy validation imports lack this field; a local identity file is not a signature against later tampering. No held-out inference or independent labels were used; this is scoring-integrity preparation, not accuracy evidence.

A read-only Daytona listing at 06:15 UTC showed one started, private, non-GPU capacity-staging sandbox (`b6b4e857-1b2c-4dbb-b61c-914188b6c1b7`) and zero GPU sandboxes. That listing is historical. Its `autoDestroyAt=2026-09-15T12:02:54Z` has passed; the provider API key was not bound to this verifier shell, so its current state is unverified. No sandbox lifecycle action or new upload was made. The loopback-only local CVAT service remains open at `http://localhost:8080/tasks`, serving all 18 frozen 100-second clips from a read-only share without reupload or GPU. It has 9,297 sampled frames and 18 incomplete review templates; the independent-label gate still reports 0 / 18 complete tasks. The CVAT export converter now hashes both local clip and parent source before exclusive label publication, but XML itself lacks a media hash, so the reviewer must confirm the task footage.

At 18:33 UTC on 15 September, a read-only query of the live CVAT database found exactly 18 frozen pilot tasks and 18 jobs, all still in `annotation`, with zero saved shapes, tracks, tracked shapes, labeled images or intervals. None of the 18 declared locked-label paths exists. This confirms no annotation work has been saved yet, beyond service/media setup. A separate read-only inspection of the five saved validation matches found two generated color clusters per match, `requiresTeamSelection=true` and no selected `myTeamCluster`; all 17,381 saved player rows across the three `117093` task results have track IDs in those clusters. Team assignment is therefore awaiting a declared human home/away mapping, not absent cluster generation. Cluster coverage is mechanical and does not establish team-label correctness or stable identity; no stored prediction artifact was changed.

Team-mapping integrity remains a human/provenance gate. The frozen protocol requires a source-specific `my_team`/`enemy` to home/away choice before prediction access. The task/source scorer now requires one source declaration file instead of a late `--my-team` flag, derives mapping before opening predictions, stamps its byte-level SHA-256 in task results and rejects a changed declaration when pooling. A shared human-truth reader rejects duplicate JSON keys in declarations, review sidecars and locked labels; lock/declaration timestamps must be RFC3339 UTC and not in the future. This closes CLI/file-binding and ambiguous-input defects, not human blindness or timing proof: no real declaration, locked v3 label or independent reviewer attestation exists yet. Read-only validation inspection found that lower-brightness centroid IDs differ across the three `117093` task outputs, so numeric `myTeamCluster` IDs are not source-wide semantic team labels. No cluster was selected and no match was reprocessed. A separate independent reviewer must declare the side from prediction-free footage and attest its timing before any held-out worker run or acceptance claim; our inspected validation cluster summaries cannot be used as blind truth.

A validation-only team join used the three saved `117093` match clusters from the read-only pilot database (SHA-256 `dba81d58d27348a41d0db0ec485eb17ac396e588fd5a02a856462f9e3e85c554`) and reproduced all frozen 5 m positional-match totals before comparing official `teamId` values. All 17,381 evaluated player rows had cluster membership, but only 12,511 / 33,000 official player positions matched. Choosing each task's cluster-to-team mapping *after* viewing official labels yields a selected-match oracle agreement of 11,221 / 12,511 (89.69%); a single best numeric mapping reused across the tasks falls to 7,982 / 12,511 (63.80%) because cluster IDs reverse. At 2 m, the optimistic conditional agreement is 9,594 / 10,361 (92.60%) with lower coverage. This is not blind home/away truth or acceptance-qualified team accuracy. The scorer already uses semantic `my_team`/`enemy`, so no cluster-ID pooling code fix was needed; the labeling runbook now requires a separately evidenced, post-lock task-local cluster selection through the existing config PATCH/reprocessing route before scoring. All three saved validation configs remain unselected, no held-out source was opened and independent labels are still absent.

The source scorer now requires complete ball/event/pitch counts, explicit event false-positive/false-negative arithmetic and pitch eligibility, and observed/inferred/unknown ball-source totals equal to predictions. It rejects duplicate keys in saved task-result JSON. This closes missing-counter-as-zero defects under frozen protocol v3; it does not authenticate a locally editable task-result file. Any eventual acceptance requires retained locked labels and native prediction artifacts plus a reproducible task-scorer replay, not a passing source JSON alone. The tests are synthetic and independent labels remain absent.

At 18:41 UTC, an unauthenticated real-browser check exposed a CVAT access defect masked by the earlier HTTP 200 page check: the UI remained on “Connecting…” because `/api/server/health/` returned 500 solely for its default 90% disk-usage threshold. The host was at 90.4% with approximately 14 GiB free; database, caches, memory and OPA checks were working. The existing local CVAT Compose override now sets the documented `CVAT_HEALTH_DISK_USAGE_MAX=95` for `cvat_server` only, retaining a 5% free-space health reserve rather than deleting user-owned datasets or disabling the check. After recreating that server, the full health response returned 200 with `DiskUsage: working`, and the browser advanced to the expected login screen. An existing active `pilot_admin` account is present, but no authenticated job-frame render was tested. Task 13/job 13 still maps to 500 sampled frames (`0..2495`, step 5) from its read-only mounted 2,500-frame, 4096×1080 clip; a directly decoded midpoint frame was visually intact football footage. CVAT remains local and open. If free space drops below the 5% reserve, the health gate will fail again; independent labels remain 0 / 18.

At 18:46 UTC, CVAT's own `TaskFrameProvider` read task 13's sampled frame indexes 0, 250 and 499 from its cache-backed, read-only media path. All three returned `image/png` at 4096×1080 with expected clip-frame mappings 0, 1250 and 2495. The actual provider-served midpoint PNG was visually intact and compared with independently decoded clip frame 1250 at SSIM 0.998327 (one-frame media-correspondence check; decoder outputs were not byte-identical). This proves backend frame serving for this one job, not authenticated browser rendering, independent annotation or model accuracy. The probe populated normal CVAT media chunks but did not create labels.

At 18:50 UTC, an in-process Django test client exercised task 13/job 13's actual HTTP view under CVAT's existing `pilot_admin` user via `force_login`, without changing the user's password or creating an account. The anonymous job-frame request returned 401; the admin request returned 200 with an 8,591,075-byte, 4096×1080 PNG. Authenticated job detail, data metadata and first compressed media chunk each returned 200; the chunk was `video/mp4` and 1,808,614 bytes. This confirms the server's permission and job-media API paths in-process, not a real login or a rendered browser canvas. Normal media chunks were cached; locked labels are still absent.

`pilot_admin` is a superuser, so this probe did not establish ordinary annotator permissions or browser rendering for any job.

At 18:53 UTC, the same in-process admin test client requested the first compressed media chunk for every frozen job. All 18 / 18 returned HTTP 200, nonempty `video/mp4` bodies (793,755–3,360,422 bytes). This exercises the initial job-media HTTP path across the complete corpus, including held-out **media only**; it does not test full-clip playback, browser rendering, ordinary annotator permissions, held-out inference or label quality. CVAT's media cache rose to 187.1 MB; host free space remained approximately 14 GiB, the all-working health response remained 200, and live labeled-shape/track tables remained empty.

On 16 September, a corpus-wide `TaskFrameProvider` probe read the first, middle and last sampled indexes from every frozen job: **54 / 54** original PNGs matched the frozen task names, source-global cadence and image dimensions across **18 / 18** jobs. All 18 mounted read-only MP4s also matched the frozen annotation-clip manifest's SHA-256 and byte size. The ignored [media QA receipt](../../backend/storage/football-analysis-pilot-cvat-v2.70.0/media-frame-qa-20260916.json) preserves the 54 frame hashes, 18 clip hashes and manifest digests. CVAT health was HTTP 200 after normal cache generation with about 13 GiB free; a read-only database audit still found 18 frozen jobs, 0 assignees and 0 saved images, shapes, tracks, tracked shapes or intervals. This is backend media/provenance verification only, not per-frame independent pixel correspondence, ordinary-reviewer access, locked labels, held-out inference or football accuracy.

A subsequent read-only second-decoder check rehashed all 18 mounted clips and all 54 CVAT original PNGs against that media QA receipt, then compared the PNGs with OpenCV decodes sought to the same frozen clip-frame indexes. Every seek reported the requested frame plus one as its next index. Across **54 / 54** sampled frames, the maximum mean absolute pixel difference was **2.5113 / 255** and the minimum downsampled grayscale correlation was **0.999338**; decoder pixels were not byte-identical. The ignored [comparison receipt](../../backend/storage/football-analysis-pilot-media-pixel-qa-20260916/all.json) is SHA-256 `4b678caeb3a53d487742b530ee10fe6f8f55c8183fba08fe548c13a07ba8d214`, and its throwaway [probe](../../backend/storage/football-analysis-pilot-media-pixel-qa-20260916/probe.py) remains available for replay. Live CVAT still reported health working and 18 unassigned jobs with zero images, shapes, tracks, tracked shapes or intervals. This strengthens sampled media correspondence, not full browser-frame coverage, independent labels, team mapping or accuracy.

At 19:05–19:07 UTC, a real Chromium browser loaded validation task 13/job 13 using a restricted, short-lived synthetic Django session for the existing `pilot_admin` superuser. Its canvas visibly rendered intact, changing football footage at sampled indexes 0, 250 and 499; the UI still showed zero object items. This passes one-job browser frame rendering and navigation under superuser access, not ordinary password sign-in, non-admin reviewer permissions, annotation Save/persistence, the other 17 browser jobs, full-clip playback or model accuracy. Ancillary event requests returned 403 without a CSRF cookie under the synthetic session; no annotation write was attempted. The browser was closed, the exact server-side session invalidated, and both restricted cookie-state files removed. CVAT remains open; independent labels remain 0 / 18.

At 19:16–19:23 UTC, disposable task 21 used three sampled frames of the already-mounted `117093-01` validation clip. Its first share-media request failed because a plain multipart `server_files` field was parsed as an empty list; a read-only `DataSerializer` comparison showed CVAT requires indexed `server_files[0]`, and the corrected request finished with size 3. A temporary non-admin `worker` assigned to probe job 21 signed in through the real browser, rendered the frame, saved one synthetic rectangle and saw it again after reload; one shape was stored only on probe job 21. An in-process export under that worker produced a valid 1,782-byte video-XML ZIP (SHA-256 `c2e38d34283ef23783a6e7ed67c7e50ddef7452893b6f5a86f4a68cb693f922d`) with a manual frame-0 box and frame-5 `outside=1` marker. The assigned probe job API returned 200, but unassigned frozen task/job 13 returned 403, so real independent reviewers must be assigned their jobs. Browser, probe task, synthetic shape, temporary account, its eight sessions and both password copies were removed; the 18 frozen jobs still have no labels. This tests a disposable workflow, not pilot acceptance.

The bounded SoccerTrack v2 coordinate adapter was exercised against both retained GSR halves for match `117092`. It streamed 20 requested frames per half without a full-file `json.load`, producing 40 reference frames, 858 usable positioned entities, and 22 omitted-position annotations. The bridge preserved all 40 unique cross-half frame IDs plus 3,142 BAS events and passed its contract audit. The BAS period parser now follows the retained file's global match clock, correctly identifying bare `90:01` and later actions as period 3 instead of period 2; only global-time windows covered by the two retained videos are scored. These are imported ground-truth labels: `groundTruthReferenceOnly=true` and `referenceLabelsUsedForInference=false`. A bounded pipeline diagnostic now exists below; acceptance-qualified alignment, HOTA/IDF1, and independently labeled accuracy remain unproven.

After that source-bound baseline, the official matching first-half panorama was fetched into ignored dataset storage: 6,687,957,167 bytes, SHA-256 `9fabe074b0c6fc5812a602dd4119f3e6cb724e2f06b8a3a1eb9358b9734c036e`, 3840x1906, 25 fps, 67,375 frames, and 2,695 seconds. A non-seeded diagnostic ran the existing release pipeline over a re-encoded 100-frame / 4-second slice and compared its 20 sampled frames with 418 usable GSR entities. The first comparison incorrectly applied a planar homography directly to the raw fisheye panorama; its 27-match / 6.5%-recall pitch result is superseded and must not be used.

The corrected [calibrated alignment scorer](../../backend/scripts/run_football_external_soccertrack_calibrated_alignment.py) projects each predicted raw-image foot point through `undistortPoints(K,D,Knew)` before applying the inverse shipped pitch-to-calibrated-image homography. The shipped mapping fails the diagnostic's 5 m p95 tolerance on both validation matches. A direct calibrated-pixel-to-pitch repair was therefore tested with 65 leave-one-keypoint-out folds per source. `117093` passes the withheld check at 2.75 m median and 4.68 m p95; `117092` fails at 3.71 m median and 8.33 m p95. The frozen task clock concatenates the half videos, but the official tracker uses an independent match clock; its metadata shifts the second-half reference lookups by +5.000 s on `117092` and −5.033 s on `117093`. Applying this correction to the existing `117093` predictions yields 71.98% pooled precision and 37.91% recall at the loose 5 m frame-local association threshold. Per-task median/p95 errors are 0.83/3.59 m, 0.83/3.07 m and 0.81/3.54 m, so every task still fails the 3 m p95 pilot bar and coverage remains low. Every input is hash-bound in the [calibration diagnostic](../../backend/benchmark_suites/football_analysis_pilot_soccertrack_calibration_cv.json) and [pitch diagnostic](../../backend/benchmark_suites/football_analysis_pilot_soccertrack_pitch_diagnostic.json); no held-out source was accessed and runtime calibration remains unchanged. Existing pitch results remain diagnostic only and excluded from readiness acceptance:

At 19:29 UTC on 15 September, a bounded fit-selection spike verified the validation calibration input hashes against the frozen diagnostic and reran 65 withheld-keypoint folds per source. The fisheye-corrected plain-fit p95 reproduced 8.328 m (`117092`) and 4.675 m (`117093`). Raw-pixel homography worsened p95 to 15.161 m and 12.285 m. RANSAC after fisheye correction at a 12 px calibrated-image threshold gave 9.771 m and 4.651 m; at 3 px it gave 18.368 m and 10.387 m. Far-side pitch boundary corners recur among the worst withheld points. No candidate clears both the 5 m assignment check and frozen 3 m pilot pitch bar, so no runtime calibration was promoted and no held-out source was touched. This is candidate rejection, not independent football accuracy.

A follow-up 65-fold nonlinear mapping probe on the same standardized, fisheye-corrected validation points found quadratic p95 pitch errors of 19.197 m and 14.422 m, and cubic p95 errors of 8.626 m and 4.682 m (`117092`, `117093` respectively). Cubic medians improved, but the withheld tails did not clear the declared bars. No polynomial model or runtime change was promoted.

- default 1280 tracking: 29 / 171 predictions matched 418 references within 5 m; precision 17.0%, recall 6.9%, 142 unmatched predictions, and 389 unmatched references;
- 1920 tracking: 61 / 308 predictions matched within 5 m; precision 19.8%, recall 14.6%, 247 unmatched predictions, and 357 unmatched references;
- 1920 increased total processing time from 30.86 s to 51.72 s and tracking time from 21.30 s to 42.19 s on the same slice, so it was not promoted globally before G-CAPACITY.

A further read-only, validation-only probe tested one non-projective hypothesis: fit the same direct homography, then interpolate its residuals with the already-installed SciPy thin-plate-spline `RBFInterpolator`. Each leave-one-keypoint-out fold trained both stages without the withheld point. With the same 65 official points and hash-bound inputs, withheld median/p95 errors fell from 3.71/8.33 m to 0.70/2.92 m on `117092` and from 2.75/4.68 m to 0.22/0.79 m on `117093`. This does **not** establish robust calibration: withholding an entire pitch third rather than one nearby point raised corrected pooled p95 to 15.73 m across x thirds and 11.07 m across y thirds on `117092` (10.86/7.41 m on `117093`). On already-saved `117092-01` whole-source predictions, post-inference 5 m frame-local matching rose from 1,296 to 2,024 of 2,766 predicted players against 10,995 official positions (precision 46.85% to 73.17%, recall 11.79% to 18.41%), but corrected matched pitch error p95 was still 3.72 m, above the frozen 3 m pilot bar. The prediction `raw_rows.json` SHA-256 is `4f952ba21356a5a00d8c9b1bcd383eeec73c0a36e51bc7ef747ff7c7da78aa8a`; the video-clock-aligned official reference SHA-256 is `f9fe410dc2991c041cc8a587e5dd63b8383232faeb5ee40cb62cc2ed70222edf`. This candidate was not inserted into runtime, does not score image-space boxes or identities, has not been tested on held-out sources, and cannot replace independent pilot labels.

A shell-only smoothing sweep on the same hash-checked validation keypoints tried five residual-interpolator settings. None reduced leave-entire-third x/y p95 below 13.20/32.54 m on `117092` or 9.69/11.69 m on `117093`. Its training-standardized coordinates and explicit third partition make the figures non-identical to the earlier spike, but the decision is unchanged: no residual correction is promoted without region-generalizing geometry and independent labels. The sweep did not access held-out sources or alter inference.

A follow-up validation-only location check found 25/15/25 official controls across far/middle/near pitch-width thirds in both cameras, so no third is empty. Under the full direct fit, the 21 far-boundary keypoints have 5.120 m median pitch residual on `117092` (2.804 m on `117093`), versus 1.421 m near (1.947 m on `117093`). A +1 calibrated-image vertical pixel moves the fitted far-boundary projection by a median 2.387 m on `117092` and 1.910 m on `117093`, versus 0.156/0.154 m near. This is in-sample residual and local sensitivity, not withheld accuracy or proof of label noise. It directs the next check toward independent far-side line/footpoint geometry and region-stratified pitch error, not a runtime change.

A hash-checked shell replay of the three saved `117093` pitch tasks reproduced all frozen 5 m frame-local assignment totals before stratifying by official pitch region. Far-side association is only 111 / 8,294 (1.34%) versus 6,161 / 8,021 (76.81%) near-side; just 155 / 17,381 saved player predictions project into the far third, and none projects into its deeper 0–10 m band despite 2,106 official scored players there. This is not independent image-box recall, and mapping error can shift predicted region membership. A second, tiny validation-only CPU sample reran five fixed frames per `117093` half: four non-overlapping 1024-pixel-wide tiles at 1280 matched 50 / 57 far-side official players under the loose 5 m rule, versus 4 / 57 for fresh whole-frame 1280 and 1 / 57 for saved tracking. Its model/data hashes matched the retained resolution diagnostic and its totals matched the common scorer, but the local Ultralytics 8.4.35/Torch 2.11 stack differs from the sealed 8.4.14/2.10 GPU release; no tracking or duplicate suppression was used. The broader tiled validation p95 remains above 3 m. Prioritize panoramic far-side coverage and camera/footpoint geometry, then independently labeled boxes/identities; neither sample authorizes runtime promotion or pilot acceptance.

SoccerTrack v2's official GSR format states that it has no ground-truth detections and that `bbox_image` must not be used for image-space scoring. Therefore no image-space MOT HOTA/IDF1 result was manufactured; no MOTChallenge ground-truth file is present. Official pitch-space GS-HOTA is separately withheld because it requires role, team, and jersey attributes that the application predictions do not emit. Anonymous tracker IDs alone are insufficient. Lowering BoT-SORT's new-track threshold was tested and rejected because it produced only a marginal recall change with lower precision.

The frozen validation windows now have bounded, video-clock-aligned official pitch-reference extracts produced by [the streaming materializer](../../backend/scripts/materialize_football_analysis_pilot_reference_intervals.py): 7,500 frames per match, 329,978 total player rows, and 15,000 ball rows across ten minutes, with zero malformed locations and no held-out access. Each row retains official match time and the corresponding video timeline time; the older extracts remain archived as `.pre_video_clock`. Despite its filename, `tracker_box_data.xml` contains normalized pitch coordinates and ball state, not image boxes. These extracts contribute zero visible-ball box minutes and zero completed manual-label minutes, so they do not change HOTA/IDF1 or analyst-acceptance status.

The [pilot corpus inventory](../../backend/benchmark_suites/football_analysis_pilot_corpus.json) now admits all six frozen disjoint whole-match slots and zero completed minutes under the frozen 30-minute protocol. The protocol assigns each match three deterministic 100-second intervals centered at 25%, 40%, and 75% of verified duration, with per-match reporting and held-out isolation; 40% avoids the predictable halftime boundary. All 18 intervals are available as frame-exact, native-resolution 100-second annotation clips: 58,491 source frames and 3,709,457,465 bytes in total. Their manifest binds every source and clip hash, frame count, frame rate and dimensions; all 18 passed mechanical media checks, and midpoint images from all six sources passed visual inspection. Scoring protocol v3 and `football_analysis_pilot_labels_v3` remove v2's accidental dense-label requirement: the clips stay intact, but labels now contain only the 9,297 declared evaluation frames. The full 30-minute reporting denominator and all metric cadences remain unchanged. The fail-closed validator still confirms 0 / 18 complete tasks and 0.0 / 30.0 minutes and now rejects off-cadence or count-drifted labels. The [labeling runbook](../runbooks/football-analysis-pilot-labeling.md) maps clip-local indexes back to source evaluation frames and requires task-local persistent track identities with independent labels and no pipeline-assisted pre-labeling. The tested scoring core covers frame selection, ball/player detection, team/role agreement, possession frame/entity/transition agreement, independently referenced pitch errors, event matching and confidence intervals. Its exact-commit TrackEval adapter passed a synthetic perfect-track integration check. The task CLI validates locked labels, reads native match artifacts, records the declared team mapping, normalizes interval-local frames and runs every metric stage. Its corpus loader now carries the frozen parent `acceptanceBars` into source aggregation; before this fix, a real source CLI run would have raised on missing threshold keys even though fixture-only tests passed. The source command requires exactly the three frozen tasks with the same mapping, preserves task namespaces for multi-file matches, retains task-stratified player/tracking/possession metrics, pools acceptance denominators and samples, recalculates confidence intervals and fails closed on missing or tampered inputs. Production prediction artifacts now exist for all six validation tasks: the three `117092` windows reuse the intact-half outputs and the three `117093` windows completed separately through Daytona as matches `dd7c1e792cae4d46bf452e079ca461d5`, `dda6be8971a34f9f841008dca7ced4c4`, and `a49358f5360348a7b7135d90abb85dab`. Each has 500 evaluated frames and the full native evaluator artifact set. A development-only official BAS diagnostic on the three `117092` windows measured 53 / 909 matched passes, 5.83% precision (95% Wilson 4.49–7.55%) and 100% recall (93.24–100%); zero predicted shots missed both official shots. Root-cause inspection found that all retained possession is `unassigned`, while the event code treated unassigned tracker-ID changes as same-team passes. Current code now refuses that unsupported inference. Rerunning the same pure event function over the retained frames removes all 18,044 full-half passes without altering stored artifacts; on the frozen windows it emits no supported events, so pass recall is 0 / 53 (95% Wilson 0–6.76%) and shot recall remains 0 / 2. This is safer output but still fails the frozen event thresholds: resolved team assignment and stable ownership are prerequisites. The single-source event diagnostic is explicitly ineligible for pilot acceptance. Independent manual labels remain absent, so no full-stage or held-out pilot acceptance result exists. SoccerTrack `117092` and `117093` are validation matches, while `118575` is the untouched panoramic test. Two disjoint Belmont Media Center broadcasts fill the broadcast slots under CC0. A 5,501.7-second, 640x480 CC0 Lakewood–Largo night match digitized from VHS fills the difficult-source slot and matches the archive's hashes; visual halftime evidence corrects the archive title's erroneous St. Petersburg–Lakewood identity. All four held-out matches have unannotated pilot intervals and no pipeline run was made. The complete SoccerNet broadcast source remains NDA-gated/non-commercial.

The hash-bound event diagnostic now also measures continuity over the retained `117092` full-half frames. After excluding fallback ID `-1`, the halves contain 7,911 and 8,195 valid player IDs with median lifetimes of 4 and 5 sampled frames; 55.44% and 52.68% of IDs appear in at most five sampled frames. Their 109,001 and 124,506 player rows average only 8.09 and 9.22 per sampled frame. These are fragmentation and detection-density indicators rather than HOTA/IDF1, but they localize detector coverage and stable tracking as prerequisites before team assignment and event scoring can be credible. No detector or tracker threshold was tuned against this proxy.

The BAS event-truth rerun applies the official `117092` second-half metadata anchor (+5.000 s relative to the frozen video task clock). Its source hash and offsets are in the diagnostic; the same 53-pass / 2-shot denominators and failure conclusions remain, without changing inference.

A 25-frame-per-half [resolution, tiling and confidence diagnostic](../../backend/benchmark_suites/football_analysis_pilot_soccertrack_resolution_diagnostic.json) isolates detector scale from tracking on `117093` and binds the model, clips, predictions, calibration, intrinsics and official reference hashes. Four non-overlapping source tiles at 1280 reach 90.73% first-half recall at 64.89% pitch-space precision and 3.97 m p95 error; after second-half clock correction they reach 93.82% recall, 74.57% precision and 3.79 m p95. Raising post-inference confidence to 0.4 gives 81.60% second-half precision and 81.45% recall on that 25-frame sample, but 3.88 m p95 still fails the frozen 3 m pitch bar. These are validation-only 5 m frame-local associations, not image-space false-positive labels or pilot acceptance. Runtime remains unchanged; panoramic detection and camera mapping require repair before identity/tracker tuning.

At 19:39 UTC on 15 September, a separate production-call tracking spike used a frame-exact, non-reencoded 100-frame / 4-second `117093-01` validation slice starting at keyframe 10 s. Parent/short decoded `framemd5` hashes matched; frozen clip, base model, reference, intrinsics and calibration hashes were checked. The CPU YOLO/BoT-SORT call at 1280 versus 1920 took 11.53 versus 21.21 seconds, yielding 125 versus 288 sampled person rows across 20 evaluation frames. Against 440 official positions aligned by `videoTimelineSeconds`, **raw** 5 m frame-local association matched 57 / 125 (45.6% precision, 12.95% recall) versus 170 / 288 (59.03%, 38.64%). A validation-only official-calibration on-pitch filter matched 63 / 88 (71.59%, 14.32%, 2.54 m matched-error p95) versus 197 / 248 (79.44%, 44.77%, 4.35 m p95). The small 1280 matched tail hides 377 unmatched truths; the 1920 pitch tail fails the frozen 3 m bar. Runtime polygon gating currently covers ball candidates, while player pitch coordinates are clamped to 0–100; the official-calibration filter cannot be silently applied to the ordinary four-corner runtime mapping. The host used Ultralytics 8.4.35/Torch 2.11 CPU, not the declared 8.4.14/2.10 GPU stack. No resolution or filter setting was promoted, no held-out source was run, and this is not pilot acceptance.

A separate tracker-admission characterization used the same hash-verified `117093` validation model, clips, saved predictions, pitch references, intrinsics and calibration: five clip frames (0, 500, 1,000, 1,500, 2,000) from each of tasks `01` and `03`, with 220 official player positions. The current shell's Ultralytics 8.4.35 differs from the declared 8.4.14, so inference was repeated in an isolated environment with Ultralytics 8.4.14; inherited Torch 2.11.0+cu130 still differs from the declared 2.10.0. Under the exploratory 5 m frame-local association rule, saved tracked output matched 81 / 115 predictions to 220 references (70.43% precision, 36.82% recall). Fresh detections at the BoT-SORT new-track cutoff ≥0.25 matched 78 / 98 (79.59%, 35.45%); all fresh detections at the production detector cutoff ≥0.12 matched 118 / 165 (71.52%, 53.64%). Including 67 lower-confidence on-pitch boxes added 40 matches and 27 unmatched detections. On the first frame alone, the detector found 19 people, six below 0.25, while the saved tracker retained exactly the 13 above that cutoff. This identifies tracker admission as one coverage loss, but fresh boxes are not tracked identities, 5 m pitch associations are not image-space false-positive labels, and the small exposed validation sample plus Torch-version drift cannot authorize a runtime threshold change or pilot acceptance. No held-out labels or predictions were opened; runtime remains unchanged. Reassess admission after independent boxes and identity labels are locked.

A separate [three-frame visual spot check](../../backend/storage/football-analysis-pilot-visual-review-20260915/README.md) decoded exact validation clip frames 0, 1,250 and 2,495 from `117093-03` and inspected the saved ball proposal boxes at their source-pixel coordinates. Each proposal area is on a player's clothing or leg, with no separate ball visible inside its box; at frame 1,250 the saved artifact has six player rows although the wide source view shows more on-field players. These are qualitative, prediction-informed observations with a three-frame denominator, **not** independent labels, clip-wide ball precision, or grounds to change a detector threshold. Keep the visual artifact away from the independent annotator until labels are locked. The app's review notes and pitch marks do not themselves supply the frozen v3 pixel-box/identity files; use the [tool-neutral labeling runbook](../runbooks/football-analysis-pilot-labeling.md) and score only after its independent-label gate passes.

A further hash-checked [validation ball-position diagnostic](../reports/2026-09-13-football-analysis-readiness.md) projects the 1,500 accepted proposal centres from all three saved `117093` tasks through the same official camera mapping used for the frozen player check. Only 47 / 1,500 are within 5 m of the video-clock-aligned official ball pitch position; 1,381 / 1,500 are over 10 m away. Excluding 444 `BALLOUT` frames leaves 39 / 1,056 within 5 m, and using the proposal bottom rather than centre does not close the gap. A read-only source-box join found 1,410 / 1,500 accepted centres inside a saved player box, including 831 in its upper half; those 1,410 rows are exactly the `player_ranked` proposal family. All 47 close pitch positions came from that family, while the 90 outside-player-box proposals yielded 0 within 5 m. Thus player proximity/support can admit clothing-like proposals, but blindly rejecting every player-overlapping proposal would also erase every close validation position; neither proxy is independent image-ball truth. This prioritizes ball acquisition/acceptance repair and independent image-box/visibility labels before possession/event trust. It is **not** visible-ball precision/recall or pilot acceptance; no runtime threshold, artifact or held-out inference changed.

A separate prediction-blind, validation-only grid probe tested the same crop-trained v7.3 ball model on five fixed frames each from annotation clips `117093-01/03`, using non-overlapping 128, 192, 256 and 384-pixel source crops resized to its 256-pixel input. Model and clip hashes matched their manifests; official ball pitch positions were opened only after all 40 frame/scale inference cells completed. At the loose 5 m pitch-position rule, 128-pixel crops yielded at least one near candidate on 3 / 10 sampled frames, 192 on 3 / 10, and 256/384 on 0 / 10; the 128/192 candidate sets contained 46–73 boxes **per frame**, while the saved accepted rows were near on 0 / 10. This is candidate flooding and positional coincidence, not image-space ball precision/recall or a deployable tiling strategy. One 128-pixel candidate at task `03` frame 500 projected 1.14 m from the official ball at confidence 0.10, yet an [exact-frame visual crop](../../backend/storage/football-analysis-pilot-ball-grid-review-20260915/README.md) shows its source box away from the visibly separate ball; the crop matched decoded frame 500 pixels exactly. The host used Ultralytics 8.4.35/Torch 2.11 CPU rather than the declared 8.4.14/2.10 GPU worker. No crop size or confidence was promoted; independent source-image ball boxes/visibility must decide the repair.

The tracked `backend/release/verification/v7.3-pre-cloud.json` binds the 12 passed local gates to the latest receipt. Its thirteenth gate is the retired infrastructure-smoke command, so it remains pending and `overallPassed=false`; it must not be relabeled as the product run. The earlier source-bound G-PRODUCT record is this status entry plus the ignored application result for match `77433cfc6846433e8f888b579ccd93b4` and job `c72b00a4cf9f40439825c9ca2025374c`; it was not rerun for the corrected source.

At 19:44–19:52 UTC on 15 September, the normal local API and React app served that saved one-minute result in a real Chromium browser. Video range requests returned 206 and the match-workspace GETs returned 200; panoramic footage, pitch, timeline and review-only/team-unresolved warnings rendered, and a 25.6-second event seek showed later video and frame 128 / 359. The Season Dashboard incorrectly displayed 50% possession even though the saved 360 assignment frames contained **zero** controlled team frames. A minimal source/read-boundary correction now represents zero-denominator possession as unknown, including for historical artifacts without rewriting them; the dashboard, HTML report and tactical search consume it without inventing a percentage or crashing. Focused backend tests passed 80 / 80, focused frontend tests 37 / 37 and the frontend production build exited zero. A browser reload showed “Not measured” in the dashboard and saved three visual screenshots under the ignored match's `ui-evidence-20260915/`. This verifies one saved-result UI path and honesty fix, not current exact-source provider acceptance or football accuracy. The local API is running at `127.0.0.1:8000` against that saved result and the frontend at `127.0.0.1:5173`; CVAT remains separately open at `localhost:8080`.

Canonical Unicode finding F8 is independently reviewed and closed: smoke CLI success and command-failure stdout now use the existing canonical UTF-8 byte serializer. The historical fourth-smoke evidence remains preserved in Git history and its dated recovery report, and the active real-smoke operator path remains retired.

The verifier ran with provider mutation disabled. G-PRODUCT then used a separate explicitly authorized provider operation; no fifth infrastructure smoke occurred.

The read-only review found and then rechecked three sibling holes in the unknown-possession path. The source now returns unknown on an empty dashboard, suppresses a query-only “Possession Based” match theme, and withholds cached tactical narratives/drills in HTML export when team-controlled possession is unmeasured, with an explicit confidence note. The cached artifact remains untouched. The first attempt at the 12-gate release verifier stopped after 3,646 backend passes and one failure in an older SoccerNet planning test: that test assumed the host had at least 10 GiB free, while measured availability had fallen just below that threshold. The production storage guard was correct; the test now uses deterministic 11 GiB and 9 GiB cases and passes both readiness and block paths. The rebound verifier passed, but report reconciliation then exposed a second brittle test that demanded the historical “128 passed across 23 files” prose despite the verified 130 / 24 result. That exact-count assertion was deleted; the operational-doc file passed 56 / 56 and the final rebound release verifier passed all 12 local gates with the counts above. Both stopped-run logs remain archived. The sealed worker was **not** rerun for this source, and no held-out inference or labels were touched. After pytest temporary files cleared, a fresh `df` reading showed 13.5 GiB free; check this again before another large job rather than assuming the low-disk condition persists.

At 23:45 UTC on 15 September, a second local-only pilot API/React pair is open on loopback ports 8001/5174 with `backend/storage/football-analysis-pilot-predictions-v1/` as its storage root; the product app/UI on 8000/5173 and CVAT on 8080 remain open. The normal pilot workspace listed all three imported `117093` validation tasks and loaded task `03` with unresolved team selection, two task-local color swatches and review-only warnings. Real Chromium playback showed intact panoramic footage; ranged video requests returned 206, the match-workspace API requests returned 200 and the console had zero errors or warnings. The screenshots are preserved in ignored `backend/storage/football-analysis-pilot-ui-operational-20260915/`; the exact video-element screenshot SHA-256 is `af2e8bdae7c5c396837a31a02471bed936aac3f8cedef44789096b81cdf6cdf2`. A read-only live CVAT query found exactly 18 pilot tasks/jobs, all 18 job assignees null, and only the active superuser `pilot_admin` account. The label validator still reports 0 / 18 complete tasks and 0 / 30 minutes. The [runbook](../runbooks/football-analysis-pilot-labeling.md) gives the two launch commands and keeps the prediction-exposed operator UI separate from the independent annotator's CVAT jobs. No cluster was selected, label saved, CVAT assignment made, held-out inference started or GPU provider operation performed.

On 15–16 September, a **local-only previous-source short-clip diagnostic** reused the sealed worker request builder without calling Daytona. The exact one-minute input SHA-256 `e1b83d4231787978f70672910febe8acc3ab39589db154967f58935b48ca379b` ran at source `09130c2fd5ea0261a45219709b033fe3c7fefebd` and manifest SHA-256 `8bc5144f3d2c805f47339c7f6a2bdc438a3022869f7b1c0f20ce1ba3a5c0020f`. The unpinned host CPU stack produced 2,333 rows / 88 ball rows; its result was validated, imported into an isolated storage root as a **local** job, and served through the normal read endpoints with unknown possession and review-only truth warnings. A second CPU run inside the existing pinned-dependency worker image produced 2,830 rows / 99 ball rows over 360 sampled frames; its completion and result contracts independently validated, and the staged input still matched the declared video hash. The earlier source-bound Daytona GPU product result had 2,834 rows / 99 accepted ball frames. This comparison flags environment-sensitive output, not a causal attribution to one package, GPU-runtime parity, current exact-source provider acceptance, or labeled accuracy. All diagnostic artifacts are preserved under ignored `backend/storage/football-analysis-current-local-short-20260915/` and `backend/storage/football-analysis-current-pinned-cpu-20260916/`; no provider operation, held-out inference, label or team declaration was created. A fresh label audit on 16 September still found 0 / 18 complete tasks and 0 / 30 minutes.

The pinned CPU result was then imported as a local job in its isolated storage root (match `39c2132d59054fa7b03fbe2429606a30`, job `012abdd89f524030b1bb743eeea0c7c6`) without a remote run ID. In-process normal API reads returned HTTP 200 for detail, frames, analytics, events, benchmark, HTML report, annotations and issues; a 1,024-byte video range returned HTTP 206. The 360-frame benchmark reports 99 accepted-ball frames, **zero controlled-possession frames**, unresolved team selection, nine provisional events from one family, `fiveMinuteTruthReady=false` and `fortyFiveMinuteTruthReady=false`. The possession percentage is unknown, and no external ball-accuracy claim follows from its internal signal status. This verifies previous-source **local persisted-product behavior** in the pinned-dependency CPU image, not Daytona/GPU transport, independent labels or analyst acceptance.

An exact decoded source-frame check at frame 155 (6.2 s, 4096×1080 / 25 fps) placed one accepted observed-ball proposal box `[460.6, 651.28, 477.58, 668.2]` fully inside blue player track 9's box `[449.71, 613.17, 488.11, 705.5]`; the annotated crop visibly lands on the player's lower body. The exact-frame PNG SHA-256 is `c6c236dca7644f886f9b13bba5e511053271ce2d7007f8625620be75b48b4f86` and its proposal crop SHA-256 is `34e3502f83b3257fd7cd286ef72c8b3503a1c8f5dcdec7565fb827509e772efa`, both preserved under the pinned diagnostic's ignored `visual-evidence-20260916/`. This is one engineer-exposed false-ball example, not a precision denominator. Seek-based stills were discarded after they were found not to match source frame 155. The diagnostic also reused manual homography points ending at x=1270 on a 4096-pixel-wide panorama, so its pitch coordinates cannot validate field calibration. No support/geometry threshold was changed on this one frame; independent visible-ball boxes and withheld calibration points remain the repair inputs.

The frame-155 example is not isolated in the retained one-minute artifacts. A read-only same-frame `xyxy` containment count found **81 / 99 accepted ball rows fully inside a player box**, including **65 / 66 directly observed ball rows**, in both the earlier Daytona GPU import and the pinned-dependency CPU import bound to the previous release source. The historical raw-row/ball-layer SHA-256 pair is `1cb0968eaeefa74727797852600caf75cd121bdf86371c9903c85c352ade998f` / `c8650ac2c52d8723f57e6a8e6d8a91ff0dcd145516413a2e709686bbe9985795`; the current processor-JSONL/ball-layer pair is `6eaf14d6104377728365d04072585fc3570692883471bf82bcc488a5acfc0089` / `0661fe2af4cd8e06ad4785117e74d22c2f78b50ed45eb7340dbaaacd56324c96`. Exact decoded first/middle/last observed-frame crops (frames 10, 285 and 1445, not hand-picked by output confidence) visually placed proposals on blue-player clothing/body or a sideline person; their PNG SHA-256 values are `721f3f431e391e21b709ed59f29176576dded9cfb08af06508edb4bfe0d264f2`, `a82cc7c776057ed6f67543442337d81697d2a9717d480ab052c6799115144c9c` and `88350c8397355123ca7370830aede3ff558ff8352a15c36c243b9c6b069b594e`. True balls can overlap a player box, so containment is a **review-priority proxy**, not false-positive rate or visible-ball precision. The matching counts across CPU and older GPU do not identify which model/proposal/geometry step caused them. Prediction-blind visible-ball boxes must decide any acceptance filter; no saved result, threshold or held-out source was changed.

The ball-status honesty correction entered at `61cc876a721ed1b76f3ae5ba22d48b1e5026bdd9` and is included in selected release source `c5f664ebab73f550f26195dcac490511682c6e33`: the shared storage save/read path reports unverified **video** ball signals as `untrusted`, and the benchmark consumes that normalized summary. In-process normal API probes against both the pinned CPU and earlier Daytona saved matches returned HTTP 200 for analytics, benchmark and HTML report with `ballSignalStatus=untrusted` and `fiveMinuteTruthReady=false`; the saved analytics bytes remained unchanged (SHA-256 `a5b4af7ea8718b08794d204016d7fc3b6c0c1227dfd5f6a402cfff8ba6ff51d4` and `80dc22b900518f81adac48a49257c89ae0070cb43906ff7bf603b67ae5b6bb9b`). Tracking-JSON behavior remains unchanged. The already-running product and pilot Uvicorn processes were left open without reload and still serve the previous code; their live responses cannot be presented as proof of this correction until a controlled service refresh. The correction does not improve detector accuracy or add an independent label.

On 16 September a fresh **local-only** product API/UI pair opened on loopback ports 8002/5175 against the exact existing `backend/storage/daytona-g-product-v7.3/` saved-match root, with `PROCESSING_BACKEND=local`; the original 8000/5173 product app, 8001/5174 pilot app and CVAT were left open. The first fresh API pointed at an unrelated empty default root and returned 404, so only that self-created diagnostic pair was replaced; no saved data was removed. The corrected pair serves older-source one-minute match `77433cfc6846433e8f888b579ccd93b4`: direct and proxied analytics return `ballSignalStatus=untrusted`, match detail HTTP 200 and ranged video HTTP 206. Real Chromium rendered changing 4096×1080 football footage, the untrusted/review-only warnings and zero console errors/warnings. A dense event-marker overlap blocked the 3.6 s button with a 4.0 s button; the native [Timeline selector](../../frontend/src/components/Timeline.tsx) now reaches every saved event by label/time. Choosing the pass at 3.6 s reached frame 18 / 359 and video time 3.6 s; the [ignored screenshot](../../backend/storage/football-analysis-fresh-ui-20260916/output/playwright/page-2026-09-16T01-40-44-833Z.png) is SHA-256 `c90af3de2c5548453e0651182a44dec4fefc94e495615180b575f76341a50f2c`. This is corrected-source **local presentation** on an alternate port, not current-source Daytona acceptance or independently scored football accuracy. The original 8000 API still returns the older `trusted` default until a controlled refresh; no team choice or pilot label was created.

The next two saved-result probes were bound to source `c5f664ebab73f550f26195dcac490511682c6e33` when performed. That source is now **previous**, after the presentation-only calibration correction at `70da1c6835eef67118a68599c5c2f0752b176cfe`; neither probe is a current-release sealed-worker or provider acceptance. Their original source, manifest and output identities are retained below for provenance.

A separate 16 September **offline, then-current-source one-minute worker probe** staged the exact 83,259,906-byte input (`e1b83d4231787978f70672910febe8acc3ab39589db154967f58935b48ca379b`) in isolated ignored storage and validated the sealed request/receipt against source `c5f664ebab73f550f26195dcac490511682c6e33`, manifest `9e57fd16919f2ec1772d1adaecf55cc38fb4ee86fa8ffa6123f2660c56931df0` and pre-cloud evidence `5008b3287d713e83084ce3e191083756bd309f0d0bfe1e84bd59728078dee768` without a provider call. An initial preserved bundle requested automatic homography and failed safely at video opening; the previously accepted one-minute match instead used declared manual diagnostic corners. A fresh sealed bundle with that exact existing configuration completed through the then-current local worker on the unpinned host CPU (exit 0), with **2,333 processor rows, 27 progress events**, restored input-video hash and a validated immutable [completion receipt](../../backend/storage/football-analysis-current-source-bundle-20260916/isolated_storage_manual/.daytona/daytona-bundle-p6952wme/completion.json) (SHA-256 `eb8cda71e58cc0dc4087d24d2aed65d810a7577360da08a434126a86202b0873`; result SHA-256 `f6f27229496ed80ac3763be666635dbe2c8be5c169e91c8792c496dad68e6dec`). The validated JSONL imported transactionally into only its isolated match `8330799da4f64e559ef7b0fe1fe5be41`: ready match/completed job, 360 sampled frames, 2,333 raw rows and 12 provisional events. In-process normal API reads returned HTTP 200 for detail, frames, analytics, events, benchmark and HTML report, and HTTP 206 for a 100-byte ranged video request; analytics says `ballSignalStatus=untrusted` and the benchmark says `fiveMinuteTruthReady=false`. The saved [local diagnostic marker](../../backend/storage/football-analysis-current-source-bundle-20260916/isolated_storage_manual/matches/8330799da4f64e559ef7b0fe1fe5be41/local_cpu_sealed_diagnostic.json) explicitly records `providerOperation=false`, `gpuRuntimeParity=false` and unpinned host CPU. This closes a **then-current-source local sealed-worker/import/read engineering probe**, not exact-source Daytona G-PRODUCT acceptance, pinned CPU/GPU parity, pitch calibration, football accuracy or an independent pilot label. The original services, CVAT, frozen jobs and historical bundles were left open/unchanged.

The same isolated previous-source match is served by a separate loopback API/UI on 8003/5176, leaving the original 8000/5173 product, 8001/5174 pilot, corrected 8002/5175 product and CVAT 8080 open. The real browser displayed its 12 provisional tags, intact 4096×1080 football video (HTML duration 72.859 s), untrusted-ball/review-only warnings and unresolved team selection. Native event selection at 24.8 s and 54.6 s reached app frames 124 / 359 and 273 / 359 with exact video times 24.8 s and 54.6 s; the two visually inspected [ignored screenshots](../../backend/storage/football-analysis-current-source-bundle-20260916/output/playwright/.playwright-cli/page-2026-09-16T02-17-34-980Z.png) and [later frame](../../backend/storage/football-analysis-current-source-bundle-20260916/output/playwright/.playwright-cli/page-2026-09-16T02-18-05-104Z.png) have SHA-256 `5f1a8226ee8aa546ccbe8320675bc62e104d46735d43c602eb80bd13f92df7ba` and `5d7dd991d03a80c63e1bcebc6d51215b307d4ae2604e4a617ef98820c390d210`. Browser console had zero errors and zero warnings. The inspection browser was closed while all loopback services remained open. This extends the offline probe through **local presentation**; it does not convert host CPU output or manual diagnostic corners into Daytona acceptance, independently measured ball/pitch accuracy, genuine team mapping or pilot truth.

That UI also showed a checked next-upload auto-detect control beside the loaded match's saved manual corners and incorrectly promised a headless automatic-to-manual fallback. Source `70da1c6835eef67118a68599c5c2f0752b176cfe` now separates the next-upload control from the loaded match's saved calibration mode and instructs a manual retry if automatic detection fails. Two red-then-green regressions and the provider-disabled 133-test frontend gate cover the correction. Real Chromium on the still-open 8003/5176 pair displayed “Loaded video: manual corners supplied” while the next-upload checkbox stayed checked; the checked [ignored screenshot](../../backend/storage/football-analysis-calibration-ui-20260916/output/playwright/.playwright-cli/page-2026-09-16T02-26-21-269Z.png) is SHA-256 `aac18b56f102a6b451c2617caf98140b10c6cdf70501840f21337634e1b8edb0`, and the console had zero errors and warnings. This is current-source UI presentation against previous-source saved output, not a rerun of the sealed worker, provider acceptance or pitch-coordinate accuracy.

At immediate-predecessor runtime source `b82a43c82d68f644846f7e1c9eda5eaa81845dcd`, a fresh credential-free bundle staged the exact one-minute video (83,259,906 bytes; SHA-256 `e1b83d4231787978f70672910febe8acc3ab39589db154967f58935b48ca379b`) with manifest `ce6d79714e5f1d6e6ba6be1150d7f2a26ff691c1db49a85e8b3584999f637b4f` and pre-cloud evidence `6a33369029e09ce9852e4f4c13ce5cc78eb8f663161ee1293616449c9fd923cd`. The pinned worker image `sha256:ab50e7ded53257a579700a40e82cee20b3556c9ce7ef251a4933c93de43c94fd` ran an exact Git-archive backend snapshot (tar SHA-256 `5d12c87cbc3586f3eb6d09a7b5244fc85c9d882ee6924af5c661af9076569f8a`) in a **network-disabled local CPU** container with an 8 GiB memory limit, Torch 2.10.0 and Ultralytics 8.4.14. The retained `fa-current-pinned-source-worker-20260916` container exited 0 without OOM after about 6 minutes 37 seconds; the input-video hash was restored exactly, and normal sealed request/result/completion validation passed. Its immutable [completion receipt](../../backend/storage/football-analysis-current-pinned-source-20260916/isolated_storage/.daytona/daytona-bundle-5h7i2d_5/completion.json) is SHA-256 `4670e13bf04963c15bdafa53f1d0cc22ba68a75973808c76810d7b7d04ce2480`; the result is SHA-256 `57e45f0c3dc9a7d14297520c9e12c2d6be3ccb554a5b149c85863655643e691f`. It records **2,830 rows, 27 progress events, 99 ball rows and 360 sampled frames**. Canonical raw rows hash to `ea0bd606457fb354f90f80c3a1bfb30786668f7edb67bf38b3fc29f38bec0747`, byte-for-byte equal after canonicalization to the earlier pinned-image CPU diagnostic at source `09130c2fd5ea0261a45219709b033fe3c7fefebd`; the unpinned host diagnostic had only 2,333 rows. This local comparison narrows environment-sensitive output but does not identify one causal dependency or establish GPU parity. The validated JSONL imported transactionally into only isolated match `1b4f9a58776c426192a0c6522fea982f`: ready match/completed job, 2,830 raw rows, 360 frames and nine provisional events. Normal in-process API reads returned 200 for detail, frames, analytics, events, benchmark, HTML report, annotations and issues, and 206 for a 100-byte video range; analytics keeps the ball signal `untrusted`, possession unmeasured and five-minute truth not ready. Its [local marker](../../backend/storage/football-analysis-current-pinned-source-20260916/isolated_storage/matches/1b4f9a58776c426192a0c6522fea982f/local_cpu_sealed_diagnostic.json) explicitly records `providerOperation=false`, `gpuRuntimeParity=false` and pinned dependencies. No live app, CVAT job, held-out source, Daytona sandbox, independent label or team choice was changed. This is a current-source pinned-CPU engineering probe, **not** current-source G-PRODUCT acceptance, calibration or football accuracy.

That isolated match is now served through an additional loopback API/UI on 8004/5177, leaving the earlier 8000–8003 APIs, 5173–5176 UIs and CVAT open. Direct and proxied match reads returned 200 and the proxied 100-byte video range returned 206. Real Chromium decoded the 4096×1080 source at ready state 4 with a 72.859-second duration; the page showed saved manual corners, unresolved team selection, the untrusted-ball and review-only warnings, nine provisional recovery tags, and no saved annotations. Selecting the 25.6-second and 54.6-second events reached exact video times 25.6/54.6 and app frames 128/273 of 359. Both visually checked screenshots show intact football footage; the [25.6-second image](../../backend/storage/football-analysis-current-pinned-source-20260916/output/playwright/.playwright-cli/page-2026-09-16T03-16-37-857Z.png) and [54.6-second image](../../backend/storage/football-analysis-current-pinned-source-20260916/output/playwright/.playwright-cli/page-2026-09-16T03-17-40-566Z.png) have SHA-256 `53d184ea326973fc119f193cd751d7057a51eb3dae8e18e5c46206a43a73348c` and `c3074dc4112eaddcb29b5e21eb74952d8e2a29bf2185a9db0300cfbcebe54363`. The browser console had zero errors and zero warnings. The inspection browser was closed while the API/UI pair stayed open. This extends the current-source pinned-CPU probe through local presentation; it does not establish Daytona/GPU acceptance, team truth, calibration or football accuracy.

The same immediate-predecessor source and pinned worker image then completed a larger **five-minute local CPU** sealed run against `videos/trimed-5min.mp4` (382,602,613 bytes; SHA-256 `cd7532baf8c075d6b8e7bbc99d03ae3e16d2778be49791d8d9a4245b6586465f`) in the network-disabled, 8 GiB-limited retained container `fa-current-pinned-five-minute-20260916`. The container exited 0 without OOM after about 28 minutes 26 seconds; the input hash was restored exactly, and receipt, result and completion contract validation passed. The pipeline reported 1,698.898 seconds total: 884.949 tracking, 421.092 observed-ball probing, 392.309 recovery selection and 0.094 truth-layer finalization. Sampled resident use during the run was about 701–725 MiB, not a measured peak. The immutable completion receipt is SHA-256 `a6ef0ad5b654c9eee03b8d2612a01dbc9db979f7c1bf4553959d7b7e4e9a9f7a`; result SHA-256 is `66428b4c7cf534754611228f501f71b15c0a27b97b56e65cd5d88f32ded98008`. Its 57 progress records led to **15,880 rows, 1,516 sampled frames, 15,186 player rows and 694 ball rows**. The earlier source-bound GPU-era five-minute artifact has 15,878 rows, so the two artifacts are not byte-equivalent. The validated result imported transactionally into only isolated match `73694746ffda40469c54055fa951d3a5`: ready match/completed job, 15,880 raw rows, 1,516 frames and 51 provisional events. Normal in-process reads returned 200 for detail, frames, analytics, events, benchmark, HTML report, annotations and issues, and 206 for a 100-byte video range. Possession remains unmeasured and the ball signal remains `untrusted`. No live app, CVAT task, held-out source, Daytona sandbox, independent label or team choice changed. This extends source `b82a43c82d68f644846f7e1c9eda5eaa81845dcd` engineering coverage from one minute to five minutes; it is **not** GPU parity, exact-current-source G-PRODUCT acceptance, calibration or football accuracy.

D01 packaging cleanup is complete in predecessor source `3c8ee4738b5046fee8f79486d6ab1221f04a1b9c`. The root wheel now excludes `backend.tests*` while preserving the source tests, application, operational scripts and release data. A red-then-green metadata check and a real wheel build/install outside the checkout passed; the installed API started, `lap` imported, and the required script/release paths remained packaged. Against the retained prior wheel, the built artifact fell from 2,414,789 to 1,633,420 bytes and from 684 to 371 entries; packaged tests fell from 314 files / 3,674,138 uncompressed bytes to zero. Repository tests were not deleted. The full provider-disabled verifier then passed all 12 gates for that source; exact-current-source sealed inference remains pending because this source delta does not change worker runtime code.

D02 retired-tail cleanup is complete in predecessor source `77b0469685334e58112676fd6516596d3982203b`. In exactly the 13 historical RunPod recipe modules named by the audit, 38 functions called the permanent retirement guard and then retained unreachable statements. A structural test failed on the first such tail before implementation. The mechanical deletion removed exactly 3,868 lines after those guards, matching the audit's measured count, while retaining all function signatures, rejection calls and independently imported analysis helpers. All 13 modules compile; the structural check, operational rejection suite and affected recipe tests pass. The full provider-disabled verifier passed all 12 gates for that source. No history, model, dataset, artifact or active Daytona path was removed.

D03 exact-helper cleanup is complete in predecessor source `38db9a6d1edb99bfe14a9c35cd2f04bf73dfebdf`. Structural matching found the audit's exact groups: 97 local `load_json` bodies and 101 local `write_json` bodies were identical to the existing implementations in `football_external_real_eval_chain_common.py`. The 198 copies across 101 scripts now retain their local `_load_json` / `_write_json` names as direct aliases to those shared functions. This removes 982 helper lines and adds 101 one-line imports, a net production reduction of 881 lines. A red-then-green structural regression rejects recurrence of either exact body; the shared-helper contract, operational safeguards and representative benchmark, SoccerNet, SoccerTrack and product callers pass. Atomic, admission, validation-specific and differently behaving JSON paths were not matched or changed, and no new utility module was introduced. The full provider-disabled verifier passed all 12 gates for that source.

Backend Medium finding C4 is closed in predecessor source `b22f6e226a881f71e013c79f7e34dbcc1a57a7e5`. The shared trust-crop scorer previously emitted a possession gap only when a later controlled assignment closed it, so all-uncontrolled matches and controlled prefixes followed by a terminal uncontrolled run produced no possession-gap crop. Both boundary tests failed before implementation. A non-mutating end-of-stream sentinel now routes terminal runs through the existing gap-closing branch; the complete trust-crop suite passes 14 / 14 tests, and the full provider-disabled verifier passed all 12 gates. This corrects review-priority scoring only; it does not improve possession inference or create possession truth.

Backend Medium finding C9 is closed in predecessor source `62c18f6d54aed81e054921bf1db04762259b75ec`. Importing the core CV module previously called `cv2.imshow` and `cv2.destroyAllWindows` to infer headless mode, so a library import could touch or fail inside the native display backend. Headless mode now comes only from the declared offscreen setting or Linux display environment, with no GUI capability probe. A subprocess regression failed on both GUI calls before the fix, the focused run-guerilla/video-pipeline suite passes 217 / 217 tests, and the full provider-disabled verifier passed all 12 gates. This changes import/display selection only; it does not alter inference math or establish football accuracy.

Backend Medium finding C3 is closed in selected source `40eade8727c0eed69cbb6b397bac0ea693103f02`. The generic atomic JSON writer now retains a hard-link backup for replacements, fsyncs the backup before publication, and compensates a failed publication commit by restoring the prior path or removing a first publication. Parent-open, commit-fsync and rollback failures are injected for both create and replace; every reported ordinary failure leaves an unambiguous retry state, while a failed compensation raises an explicit do-not-retry outcome and retains recoverable prior bytes. The full provider-disabled verifier passes all 12 gates with 3,682 backend tests. This is filesystem transaction evidence, not football accuracy.

Backend Medium finding C5 is reconciled as already closed by the live-progress implementation in selected source `40eade8727c0eed69cbb6b397bac0ea693103f02`. The sealed worker validates, bounds, credential-checks and flushes each monotonic progress event immediately; the Daytona session parser delivers complete canonical events to the host callback while remote execution is still running, and job persistence/websocket/UI paths expose the current message and percentage. Focused replay passes 7 / 7 Daytona progress tests, 15 / 15 worker progress tests and 22 / 22 application tests. Historical G-PRODUCT and five-minute provider runs visibly reached intermediate stages before completion; no current-source provider call was made for this reconciliation. Live telemetry proves availability behavior, not football accuracy.

Backend Medium finding C6 is closed in selected source `40eade8727c0eed69cbb6b397bac0ea693103f02`. Bundle listing raises a distinct corruption error for malformed JSON, invalid UTF-8 or schema-invalid data instead of silently omitting the file. The existing per-instance locking pattern serializes bundle read-modify-write updates and deletion, so two concurrent non-conflicting field updates survive. Deletion now atomically renames the bundle, fsyncs the visible removal and restores it on a failed commit; a failed rollback is explicit and retains the bytes under the tombstone. Focused storage/API/research bundle checks pass 35 / 35 tests. The documented locking ceiling remains multiple `Storage` instances or API worker processes. This protects review data only and does not create football truth or accuracy evidence.

Backend Medium finding C7 is reconciled as already closed by remediation unit R08 in selected source `40eade8727c0eed69cbb6b397bac0ea693103f02`. The research add-on derives its boundary from the installed package or actual checkout, uses resolved path ancestry rather than string prefixes, rejects the general temporary root and nonexistent arbitrary temporary paths, and permits only its dedicated root or an existing effective-user-owned temporary directory. `run_judge` enforces the resolver internally before dry-run or delegation, so direct callers and both CLIs cannot bypass it. Alternate-checkout, sibling-prefix, traversal, protected-subtree symlink, installed-package, generic-temp, direct-call and CLI regressions pass 84 / 84 focused tests; the complete sidecar passes 100 / 100 tests in the receipt-bound verifier and a fresh focused replay. This is path-confinement evidence, not proof that delegated football analysis is accurate.

Backend Medium finding C8 is closed operationally in selected source `40eade8727c0eed69cbb6b397bac0ea693103f02` without weakening the 24-hour freshness policy. The provider-neutral preflight now has a read-only `check-freshness` command that validates the canonical evidence phase and fails before the oldest gate enters a declared renewal window; the live four-hour check reported 85,758 seconds remaining. The Daytona runbook requires monitoring that command and renewing only by settling source, binding a manifest, rerunning the complete provider-disabled verifier, publishing new receipt-bound evidence and revalidating exact build inputs. Editing timestamps, copying an old receipt, reducing maximum age and starting a provider job are explicitly forbidden renewal mechanisms. The focused evidence/preflight/docs suite passes 327 / 327 tests. This is an operator warning and renewal path, not automatic in-app attestation or Daytona acceptance.

Backend performance finding P3-runtime is closed in selected source `40eade8727c0eed69cbb6b397bac0ea693103f02`. The SoccerNet video and label range extractors now share one standard-library streaming path from `zf.open()` through `shutil.copyfileobj`, require the approved uncompressed size to match ZIP metadata, bound every output write to that exact size and publish only a complete temporary file. Their inventory hashes are also streamed instead of rereading the complete member into memory. A 64 MiB compressible fixture succeeds under a hard 48 MiB child-process address-space limit; overlong and short hostile streams fail without a partial destination. The complete SoccerNet script sweep passes 185 / 185 tests, and the receipt-bound verifier passes all 12 local gates. This is acquisition resource-safety evidence, not football accuracy or a fresh external download.

Backend performance finding P4-runtime is closed in selected source `40eade8727c0eed69cbb6b397bac0ea693103f02`. `save_analytics` now persists the normalized `MatchSummary` in one nullable SQLite column after the full artifact is durably written; dashboard and tactical search read that compact index instead of parsing every assignment, formation and shot array. Existing databases add the column in place, and a missing or invalid historical index falls back to the full artifact once and backfills itself. The regression benchmark returns 2,000 indexed completed matches in under two seconds with zero full-artifact reads; the focused storage/search/API suite passes 115 / 115 tests and the receipt-bound verifier passes all 12 local gates. This changes query cost, not football inference or accuracy.

Frontend Medium findings COR-04, COR-08 and COR-09 are closed in predecessor source `4aeeb73efb48768a3567b4ae497e308bf12efb37`. The Tactical Brain now starts on its first rendered `analysis` tab instead of the nonexistent `events` view. A failed playlist deletion remains contained in the review-bundle hook that already owns its visible error instead of rejecting an intentionally discarded UI promise. Annotation and issue list failures now surface the API error rather than replacing failed loads with indistinguishable empty lists; the latest callback is retained without making callback identity a refetch trigger. Each boundary failed under a focused regression before its minimal fix.

Frontend findings COR-06, COR-10 and COR-11 are closed in selected source `bc62d1b0a0352a946135f4c5b5503fb271fce2a1`. The shared workspace transition now closes a selected-player panel before displaying another match, preventing an old identity from being paired with new-match events. Trust-crop save failures have their own visible error and no longer hide the already-loaded queue or claim loading failed. Both date renderers now test the native `Date` value and preserve malformed source text instead of leaking `Invalid Date`. All four new behavior regressions failed before their minimal corrections.

Current code also confirms earlier corrections for COR-05, COR-07 and COR-13: player hit-testing uses current-frame positions and excludes absent profiles; native video play, pause, end and rejected-play paths synchronize application playback state; saved pitch annotations render only inside their inclusive saved timestamp range. Focused behavioral tests cover all three boundaries.

Frontend findings A11Y-04 and PERF-03 are closed in selected source `40eade8727c0eed69cbb6b397bac0ea693103f02`. Both match selectors now have programmatic names; overlay and drawing-mode toggles expose their pressed state; calibration coordinate inputs retain explicit corner/axis labels; and every visual event marker has a descriptive accessible name. The stable creator, finisher and ball-winner rankings are memoized by the existing `playerProfiles` reference rather than cloned and sorted again on playback-driven parent renders. Five regressions failed before the minimal native/React fixes. The selected frontend gate passes 144 / 144 tests across 26 files together with lint, both TypeScript checks and production build; the full provider-disabled verifier passes all 12 gates. This improves interface access and playback render cost only; it does not add football truth or accuracy.

### Deliberate handoff boundary

This is the safe pause before the evidence-producing human pilot. Engineering preparation, release verification, frozen media, CVAT serving, label conversion/validation and scoring paths are ready, while the fail-closed label audit remains **0 / 18 tasks, 0 / 9,297 evaluation frames and 0 / 30 minutes**. The next action must come from a named independent reviewer: make prediction-free source declarations, accept assigned CVAT jobs, review and lock every task, and only then permit held-out inference and scoring. Starting held-out inference, choosing teams from inspected validation outputs, fabricating declarations or filling labels from predictions would cross the protocol boundary and invalidate the intended acceptance evidence. Current-source Daytona acceptance, arbitrary-source scale, calibration, identity, ball, possession and event accuracy therefore remain unproven. The loopback product/pilot services and CVAT are intentionally left open for that handoff.

## Remaining sequence

The passed G-PRODUCT and G-CAPACITY entries below are **historical source-bound results**; neither has current exact-source Daytona acceptance. The local CPU diagnostic above does not change those gate statuses.

1. **G-PRODUCT — passed:** `videos/trimed-football-2-1minute.mp4`, SHA-256 `e1b83d4231787978f70672910febe8acc3ab39589db154967f58935b48ca379b`, ran through the actual sealed Daytona worker. Intermediate progress reached sequence 25 / `resultSerialize` / 98%; validated import produced 2,834 rows, 360 frames, 99 accepted ball frames, and 54 events; detail, frames, analytics, events, report HTML, and benchmark endpoints all returned 200. Worker processing took 83.714 seconds. The job completed, the match is ready, and an independent Daytona listing confirmed zero remaining sandboxes. Three representative source frames were visually checked as intact panoramic football footage. This proves only the bounded product path, not independent accuracy.
2. **G-CAPACITY — passed for representative match `117092`:** both exact intact halves completed sequentially through the unmodified production command. The 6,687,957,167-byte first half (SHA-256 `9fabe074b0c6fc5812a602dd4119f3e6cb724e2f06b8a3a1eb9358b9734c036e`) ran as job `3d6c2997771b469482e08f46fe302c7f`, match `c43b571fdb1745a08b8209430bb65abe`, processing all 67,375 source frames into 13,475 sampled frames spanning 0.0–2,694.8 seconds, 109,001 player rows, 13,089 accepted ball rows and 9,467 provisional events. End-to-end time was 8,378.199 seconds and pipeline time 7,605.868 seconds. The 6,690,296,959-byte second half (SHA-256 `eb088f442ca0ca30abb86b6c402b381ad6707d9eb1c40c62785e84e34aecc459`) ran as job `875634344e094ba3a86d8b691993f357`, match `5b56cbd876664b9b9fe25b1c95515b8f`, processing all 67,550 source frames into 13,510 sampled frames spanning 0.0–2,701.8 seconds, 124,506 player rows, 12,773 accepted ball rows and 9,297 provisional events. End-to-end time was 8,145.772 seconds; its 7,645.972-second pipeline comprised 5,165.877 seconds tracking, 1,070.400 seconds observed-ball probing, 1,407.381 seconds recovery selection and 1.889 seconds truth-layer finalization. Across both halves, worst measured peaks were 2.219 GiB worker RSS, 1.008 GiB host RSS, 849 MiB total sandbox GPU use, 6.452 GiB local staging and 8.000 GiB remote staging. Each ephemeral GPU sandbox was removed, the explicitly retained private CPU staging sandbox was preserved, zero unexpected sandboxes remained and monitor errors were empty. Source and overlay checks at the beginning, midpoint and end of each half show intact footage and broadly aligned player boxes, with some sideline/bench detections, missed distant players and visually ambiguous ball candidates. Manual diagnostic corners, unresolved team selection and provisional events exclude all output counts from accuracy claims. Chunk assembly is unnecessary for this measured envelope; temporal splitting remains a fallback, not the primary design.
3. **Validation predictions — complete for 6 / 6 tasks:** the three frozen `117092` windows reuse the already imported intact-half artifacts. The three `117093` 100-second clips completed through the sealed worker as jobs `f0043d55432749c6a6a054d79c5393c1`, `c1b6aba5a705472fb21890a37ca6fc54`, and `baef36beb24448609d19c93b2a2de08f`; every job completed, every result imported, and every ephemeral GPU sandbox was removed. The previously retained CPU staging sandbox's current state is unverified. The outputs total 1,500 evaluated frames and 18,881 raw rows. They are ready for the task evaluator after independent labels and a declared team mapping; they are not accuracy evidence. No held-out source has been run.

G-NETWORK remains conditional on any non-local deployment, and G-RETENTION remains conditional on an exact kept/archived/deleted manifest. Remaining Medium/Low findings stay open unless explicitly closed above. Existing historical evidence, videos, models, datasets, and application data remain preserved.
