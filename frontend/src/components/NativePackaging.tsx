export default function NativePackaging() {
  return (
    <section aria-label="Native packaging" className="rounded-lg border border-slate-700 bg-slate-900 p-3 text-xs text-slate-300 space-y-1">
      <h3 className="text-[11px] uppercase tracking-wide text-slate-500">Native packaging</h3>
      <p>Native wheels are not universally portable. Accelerator engines stay unadmitted.</p>
      <p>macOS and Ubuntu profiles are tested independently; neither profile is currently proven.</p>
      <p>Exact FFmpeg build notices are required. A wrapper does not remove licence obligations.</p>
      <p>No RPC fleet moves full-resolution frames. Custom native code remains unapproved.</p>
    </section>
  );
}
